import {Metadata} from "next";
import Main from "./components/Main";

export const metadata: Metadata = {
  title: "Register"
}

const Page = () => {
  return (
    <><Main /></>
  )
}

export default Page